CISC 3300 S25 
In class assignment 6 
1. CSS FRamework is a collection of CSS stylesheets that are prepped and ready to use. 2. Bootstyle components are pre-styled UI elements like buttons, forms, navbars, and more, built with HTML, CSS, and JavaScript. 
3. Responsive design creates dynamic changes to the appearance of a website, depending on the screen size and orientation of the device being used to view it. 
4. Viewport (meta) is needed to get media queries to work 
5. Create a mobile collapsible bootstrap header: 
a. on https://getbootstrap.com/docs/5.3/getting-started/introduction/ the documentation grab three of the CDN links, the cdn urls should be: 
i. https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css ii. https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.j s 
iii. https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.j s 
b. copy the link element and two script elements and include them in a head element on a new HTML page, also include a viewport meta tag 
c. access the navbar component documentation: 
https://getbootstrap.com/docs/5.3/components/navbar/ and copy the example code under the “Supported Content” header 
d. update the brand and links to be from your fictitious business(they can be whatever occurs to you, just show that you know how to change the links from the template). Also change the color if you wish, notice there are helper classes for that listed on the documentation page.